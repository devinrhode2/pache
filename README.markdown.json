# `pache`

Start apache in any dir frm COMMAND LINE

Install with npm. Npm comes with node, install it here: http://nodejs.org/
```
sudo npm install pache -g
```
Run it:
```
pache site-directory 2000
```